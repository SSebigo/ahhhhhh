import 'package:flutter/material.dart';

/// @nodoc
class HomeDrawerSeparator extends StatelessWidget {
  /// @nodoc
  const HomeDrawerSeparator({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      height: 1.0,
      width: double.infinity,
      color: Colors.black,
    );
  }
}
